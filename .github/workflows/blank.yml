name: Keep Bot Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # каждые 5 минут
  workflow_dispatch:        # возможность запускать вручную

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl request to bot
        run: |
          curl -X GET https://e343fd4e-2851-4c71-8e5c-a50fd1771123-00-1mxzw1wqzgcrr.picard.replit.dev/
